<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\JobTracker.Modules.Jobs.Application\JobTracker.Modules.Jobs.Application.csproj" />
    <ProjectReference Include="..\..\..\Common\JobTracker.Common.Infrastructure\JobTracker.Common.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" />
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
    <PackageReference Include="EFCore.NamingConventions" />
    <PackageReference Include="MediatR" />
    <PackageReference Include="FluentValidation.DependencyInjectionExtensions" />
  </ItemGroup>

  <ItemGroup>
    <!-- The DbContext, the configurations and the repositories are internal:
         nothing outside the module may reach the module's tables. The
         integration tests get an explicit grant rather than the types being
         widened to public for the convenience of a test project. -->
    <InternalsVisibleTo Include="JobTracker.IntegrationTests" />
    <InternalsVisibleTo Include="JobTracker.ArchitectureTests" />
  </ItemGroup>

</Project>
